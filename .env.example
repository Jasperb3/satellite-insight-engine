# Google Earth Engine project id (run `earthengine authenticate` first)
GEE_PROJECT=your-gee-project-id

# Nominatim (OpenStreetMap) user agent — use a unique string for your app
NOMINATIM_AGENT=satellite-insight-engine

# Ollama hosted web search (optional). If unset, enrichment falls back to free sources.
OLLAMA_API_KEY=

# Optional enrichment keys — all gracefully skipped when absent:
TAVILY_API_KEY=        # recent news (https://tavily.com)
EXA_API_KEY=           # reserved: neural search (https://exa.ai)
NASA_API_KEY=          # reserved: EONET events & GIBS imagery need no key
WEATHER_API_KEY=       # reserved: OpenWeatherMap. Weather uses keyless Open-Meteo by default

# Models (already pulled locally)
VISION_MODEL=minicpm-v4.5:q8_0
AGENT_MODEL=lfm2.5:latest

# Behaviour
RETENTION_DAYS=30
DEFAULT_BUFFER=1500
